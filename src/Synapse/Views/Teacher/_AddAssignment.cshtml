@model Synapse.Data.ViewModels.GradesVM

<div id="assignment" hidden>
    <div class="container has-text-centered" style="margin-bottom: 2rem; width: 100%;">
        <h1 class="title">View Assignments</h1>
        <hr />
        <table class="table" style="width: 100%; margin-left: auto; margin-right: auto">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Category</th>
                    <th>Due Date</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach(var assignment in Model.ClassAssignments)
                {
                    <tr>
                        <td><input class="input" type="text" value="@assignment.AssignmentName" /></td>
                        <td>
                            <div class="select" style="width: 100%">
                                <select style="width: 100%">
                                    <option>@Model.AssignmentCategories.Find(c => c.CategoryId == assignment.CategoryId).CategoryName</option>
                                    @foreach(var c in Model.AssignmentCategories)
                                    {
                                        if(c.CategoryId != assignment.CategoryId)
                                        {
                                            <option>@c.CategoryName</option>
                                        }
                                    }
                                </select>
                            </div>
                        </td>
                        <td><input class="input" type="date" value="2019-02-10" /></td>
                        <td><a class="button is-danger">Delete</a></td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <div class="container" style="margin-bottom: 2rem;">
        <h1 class="title has-text-centered">Add Assignment</h1>
        <hr />
        <form>
            <div class="columns">
                <div class="column">
                    <div class="field">
                        <label class="label">Assignment Name</label>
                        <input class="input" type="text" placeholder="Enter Assignment Name..." />
                    </div>
                </div>
                <div class="column">
                    <div class="field">
                        <label class="label">Assignment Category</label>
                        <div class="select" style="width: 100%">
                            <select style="width: 100%">
                                <option>Test</option>
                                <option>Quiz</option>
                            </select>
                        </div>
                    </div>
                </div>
                <div class="column">
                    <div class="field">
                        <label class="label">Assignment Due Date</label>
                        <input class="input" type="date" />
                    </div>
                </div>
                <div class="column">
                    <div class="field">
                        <label class="label">Actions</label>
                        <input class="input button is-primary" type="submit" value="Submit" />
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>